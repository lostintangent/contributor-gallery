{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "attach",
            "name": "Chrome",
            "preLaunchTask": "start",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}