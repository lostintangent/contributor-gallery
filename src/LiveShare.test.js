import React from 'react';

it('allows real-time editing (with auto-completion!)', () => {
});

it('supports collaborative debugging (with shared stepping!)', () => {
});

it('allows sharing locally running web apps (using a secure channel!)', () => {
});

it('enables sharing remote terminals (with either read-only or read/write access!)', () => {
});