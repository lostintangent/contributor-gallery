import styled from "styled-components";

export default styled.h2`
    color: ${({ theme }) => theme.secondaryColor};
    font-size: 1em;
    font-weight: bold;
    margin-bottom: 1.5em;
    text-align: left;
`;